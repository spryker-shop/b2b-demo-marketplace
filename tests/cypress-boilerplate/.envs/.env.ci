BACK_OFFICE_URL=http://backoffice.eu.spryker.local
STOREFRONT_URL=http://yves.eu.spryker.local
GLUE_URL=http://glue.eu.spryker.local
MP_URL=http://mp.eu.spryker.local
DOCKER_CLI_URL=http://cli:9000
GLUE_BACKEND_URL=http://glue-backend.eu.spryker.local
# path from tests/cypress-boilerplate (Cypress's cwd) up to the repo root, where the `docker/sdk` binary is cloned in CI
PROJECT_LOCATION=../..

# Shop data the dynamic fixtures are created for — see .envs/.env.local for details.
# This is the single place to change when the project's store set changes.
STORE_NAME=DE
LOCALE_NAME=en_US
CURRENCY_CODE=EUR
COUNTRY_ISO2=DE
DEFAULT_PASSWORD=change123