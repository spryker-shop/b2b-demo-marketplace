# yaml-language-server: $schema=../../../../Spryker/Configuration/resources/configuration/configuration-schema-v1.json
features:
    - key: theme
      name: Theme
      description: Visual branding settings for Storefront, Back Office, and Merchant Portal
      enabled: true
      order: 10
      tabs:
          - key: storefront
            name: Storefront
            icon: storefront
            description: Storefront theme colors and custom CSS
            order: 2
            enabled: true
            groups:
                - key: colors
                  name: Storefront Colors
                  scopes:
                      - global
                      - store
                  enabled: true
                  settings:
                      - key: background_brand_primary
                        name: Brand Primary
                        description: Primary buttons, cart badge, pagination active page, checkbox/radio/switch checked state, mega-menu active indicator
                        type: color
                        default_value: '#318272'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_brand_hover
                        name: Brand Hover
                        description: Hover state for primary buttons, checkbox, radio, and switch
                        type: color
                        default_value: '#226558'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_brand_pressed
                        name: Brand Pressed
                        description: Active/pressed state for primary buttons
                        type: color
                        default_value: '#15483e'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_brand_subtle
                        name: Brand Subtle
                        description: Tertiary button hover background, selected option highlight in dropdowns
                        type: color
                        default_value: '#f3fffc'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_accent_primary
                        name: Accent Primary
                        description: Secondary accent color for alternative CTAs and decorative elements
                        type: color
                        default_value: '#27514d'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_accent_hover
                        name: Accent Hover
                        description: Hover state for accent-colored elements
                        type: color
                        default_value: '#234845'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_accent_pressed
                        name: Accent Pressed
                        description: Active/pressed state for accent-colored elements
                        type: color
                        default_value: '#1b3835'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_accent_subtle
                        name: Accent Subtle
                        description: Light tinted accent background for secondary badges and highlights
                        type: color
                        default_value: '#eef7f6'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: background_highlight
                        name: Background Highlight
                        description: Accent background for highlighted sections on the page
                        type: color
                        default_value: '#f9faff'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: text_brand
                        name: Brand Text
                        description: Links, breadcrumbs, active tabs, navigation hover, shopping list names, mini-cart item links, suggest search highlights
                        type: color
                        default_value: '#226558'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: border_brand
                        name: Brand Border
                        description: Active tab underline, checkout back-button border
                        type: color
                        default_value: '#318272'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: icon_brand
                        name: Brand Icon
                        description: Brand-colored icons across the storefront
                        type: color
                        default_value: '#226558'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: focus_ring
                        name: Focus Ring
                        description: Keyboard focus outline on selects, user-account dropdown, cart pill
                        type: color
                        default_value: '#acffef'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: shadows_focus_color
                        name: Focus Shadow
                        description: Focus glow on buttons, inputs, checkboxes, switches, selects, tabs
                        type: color
                        default_value: '#acffef'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: header_topbar
                        name: Header Top Bar
                        description: Background of the top info/utility bar above the main header
                        type: color
                        default_value: '#3b3b3d'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: header_main
                        name: Header Main
                        description: Background of the main header area with logo, search, and cart
                        type: color
                        default_value: '#ffffff'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: header_nav
                        name: Header Navigation
                        description: Background of the category navigation bar below the main header
                        type: color
                        default_value: '#fafafa'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: footer_newsletter
                        name: Footer Newsletter
                        description: Background of the newsletter subscription section in the footer
                        type: color
                        default_value: '#1b3835'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: footer_main
                        name: Footer Main
                        description: Background of the main footer area with links and contacts
                        type: color
                        default_value: '#ffffff'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'

                      - key: footer_bottombar
                        name: Footer Bottom Bar
                        description: Background of the bottom bar in the footer with payment icons and copyright
                        type: color
                        default_value: '#f5f5f5'
                        scopes:
                            - global
                            - store
                        enabled: true
                        secret: false
                        storefront: true
                        constraints:
                            - type: regex
                              message: 'Must be a valid hex color (e.g. #ff0000)'
                              options:
                                  pattern: '^#[0-9A-Fa-f]{6}$'
