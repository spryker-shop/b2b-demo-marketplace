build:
    environment:
        php: '8.3'
        node: 'v18.20'

    dependencies:
        before:
            - 'npm cache clean --force'
            - 'rm -rf node_modules'
            - 'npm install'

    tests:
        override:
            - true # disable test execution on scrutinizer

    nodes:
        analysis:
            tests:
                override:
                    - php-scrutinizer-run
