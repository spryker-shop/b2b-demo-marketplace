{% extends view('availability-subscription', '@SprykerShop:AvailabilityNotificationWidget') %}

{% block body %}
    {% include molecule('availability-subscription', 'AvailabilityNotificationWidget') with {
        data: {
            isSubscribed: data.isSubscribed,
            subscribeForm: data.subscribeForm,
            unsubscribeForm: data.unsubscribeForm,
        },
    } only %}
{% endblock %}
