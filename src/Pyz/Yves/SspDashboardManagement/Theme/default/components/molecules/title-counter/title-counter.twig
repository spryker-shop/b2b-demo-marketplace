{% extends molecule('title-counter', '@SprykerFeature:SspDashboardManagement') %}

{%- block extraClass %} spacing-bottom {{ parent() }}{% endblock -%}

{% block counter %}
    {% if data.counter %}
        <span class="{{ config.name }}__counter col badge badge--counter">{{ data.counter }}</span>
    {% endif %}
{% endblock %}
